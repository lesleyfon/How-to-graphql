 type Query {
        info: String!
        feed: [Link!]!
    }
    
    type Link {
        id: ID!,
        description: String!
        url: String!
    }

    type Mutation {
        postLink ( description: String!, url: String!): Link!
    }